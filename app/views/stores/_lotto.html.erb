<p>총 판매액: &#8361; <%= @lottery.sales_in_won %></p>

<p>
	당첨번호: 
	<% @rank.rank_nums.each do |number| %>
		<% if number.special %>
			보너스 번호 <%= lotto_ball(number.num) %>
		<% else %>
			<%= lotto_ball(number.num) %>
		<% end %>
	<% end %>
</p>

<% @ranks.each do |rank| %>
	<p>
		<%= rank.rank %>등 당첨자수: <%= rank.winners_in_ko %>
		당첨금액: &#8361; <%= rank.winning_in_won %>
		당첨총액: &#8361; <%= rank.total_winning_in_won %>
	</p>
<% end %>

